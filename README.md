# C Study 🔧

하이테크 직업교육과정에서 진행한 **C언어 실습 코드 모음**입니다.
포인터를 중심으로 메모리·주소 개념을 학습한 코드입니다.

## 📂 주요 주제
- 포인터와 메모리 주소 (`address_demo`, `dereference_read/write`)
- 배열과 포인터의 관계 (`array_is_pointer`, `array_pointer_all`)
- 포인터 연산 및 활용

## ▶️ 컴파일 & 실행
```bash
gcc 파일명.c -o 출력
./출력
```
